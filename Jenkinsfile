pipeline {

    agent any

    tools { 
        maven 'my-maven' 
    }

    stages {

        stage('Build with Maven') {
            steps {
                echo 'mvn --version'
            }
        }
}
}
